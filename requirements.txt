pandas
numpy
requests
certifi
matplotlib
scikit-learn
xgboost

# Raw C3D reading and the animated swing dashboard.
# plotly needs nbformat to render a figure inline in a notebook; without it
# fig.show() raises, though save_dashboard() still writes the HTML fine.
ezc3d
plotly
nbformat>=4.2.0

# Front end for the same dashboard: streamlit run streamlit_app.py
# 1.29 is the floor the app is written against; the bordered cards and the
# coloured percentile bars need 1.45 and are skipped on anything older.
streamlit>=1.29
